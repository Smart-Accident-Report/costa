import 'package:flutter/material.dart';

class ScanAccidentScreen extends StatelessWidget {
  const ScanAccidentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}